'use client';
import { useState } from 'react';
import { money, product, subtotal } from '@/lib/product';
import { OrderButton } from './OrderButton';
export function QuantityCard() { const [quantity, setQuantity] = useState(1); const total = subtotal(quantity); return <div className="rounded-3xl bg-white p-6 shadow-xl shadow-stone-900/5"><div className="mb-5 flex items-center justify-between"><p className="text-sm font-bold uppercase tracking-wider text-stone-500">Choose your pairs</p><span className="rounded-full bg-[#edf2e8] px-3 py-1 text-xs font-bold text-moss">COD available</span></div><div className="flex items-center justify-between border-y border-stone-100 py-4"><span className="font-semibold">Quantity</span><div className="flex items-center gap-4 rounded-full bg-sand p-1"><button aria-label="Decrease quantity" onClick={() => setQuantity(Math.max(1, quantity - 1))} className="h-8 w-8 rounded-full bg-white font-bold">−</button><span className="w-4 text-center font-bold">{quantity}</span><button aria-label="Increase quantity" onClick={() => setQuantity(quantity + 1)} className="h-8 w-8 rounded-full bg-white font-bold">+</button></div></div>{quantity >= 2 && <p className="mt-3 text-sm font-semibold text-moss">Pair offer applied — save {money((quantity % 2 === 0 ? quantity / 2 : (quantity - 1) / 2) * 400)}.</p>}<div className="mt-5 flex justify-between text-lg"><span className="font-semibold">Total</span><span className="font-bold text-clay">{money(total)}</span></div><OrderButton quantity={quantity} className="mt-5 w-full">Purchase now · {money(total)}</OrderButton><p className="mt-3 text-center text-xs text-stone-500">Free inside Kathmandu Valley · Rs 100 outside</p></div>; }
