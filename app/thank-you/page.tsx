'use client';
import Link from 'next/link';
import { useSearchParams } from 'next/navigation';
import { money } from '@/lib/product';
export default function ThankYou() { const p = useSearchParams(); const total = Number(p.get('total')) || 0; return <main className="grid min-h-screen place-items-center bg-sand px-5 text-center"><section className="max-w-xl rounded-[2rem] bg-white p-8 shadow-xl shadow-stone-900/5 md:p-12"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-[#edf2e8] text-3xl text-moss">✓</div><p className="mt-6 text-xs font-bold uppercase tracking-[.2em] text-clay">Order received</p><h1 className="mt-3 font-display text-5xl">Thank you for your order!</h1><p className="mt-5 leading-7 text-stone-600">Our sales representative will call you soon to confirm your order.</p><div className="mt-7 rounded-2xl bg-sand p-5 text-left text-sm"><p className="flex justify-between border-b border-stone-200 pb-3"><span>Order ID</span><b>{p.get('orderId')}</b></p><p className="flex justify-between pt-3"><span>Product</span><b>{p.get('product')}</b></p><p className="mt-3 flex justify-between"><span>Quantity</span><b>{p.get('quantity')}</b></p><p className="mt-3 flex justify-between"><span>Total</span><b className="text-clay">{money(total)}</b></p><p className="mt-3 flex justify-between"><span>Payment method</span><b>Cash On Delivery</b></p></div><Link href="/" className="mt-8 inline-block rounded-full bg-clay px-6 py-3 text-sm font-bold text-white">Back to Home</Link></section></main>; }
